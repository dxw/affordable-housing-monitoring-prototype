<%= render partial: '/partial/header_developer' %>
<%= bootstrap_form_for([@development, @dwelling]) do |form| %>
  <%= render partial: 'form', locals: { form: form } %>
  <%= form.submit %>
<% end %>

<%= button_to 'Delete Dwelling', development_dwelling_path(@development, @dwelling), method: :delete, data: {confirm: 'Are you sure?'} %>
